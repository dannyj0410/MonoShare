const genericMessagePart = "limit reached. Please try again later.";

//Auth
export const signInLimiterMessage = `Sign in attempt ${genericMessagePart}`;
export const registerLimiterMessage = `Account creation ${genericMessagePart}`;

// Secret
export const createSecretLimiterMessage = `Secret creation ${genericMessagePart}`;
